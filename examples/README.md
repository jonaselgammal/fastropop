# Examples

This directory is for lightweight, version-controlled examples that exercise
the public API without requiring notebook execution.

Planned additions after code migration:

- basic single-population sampling example
- parameter-scan example
- benchmark example showing the JAX speedup path

Notebook-style exploratory material can live under `examples/notebooks/`.

